import { As400DatePipe } from './as400-date.pipe';

describe('As400DatePipe', () => {
  it('create an instance', () => {
    const pipe = new As400DatePipe();
    expect(pipe).toBeTruthy();
  });
});
