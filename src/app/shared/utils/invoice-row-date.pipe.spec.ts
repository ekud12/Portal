import { InvoiceRowDatePipe } from './invoice-row-date.pipe';

describe('InvoiceRowDatePipe', () => {
  it('create an instance', () => {
    const pipe = new InvoiceRowDatePipe();
    expect(pipe).toBeTruthy();
  });
});
