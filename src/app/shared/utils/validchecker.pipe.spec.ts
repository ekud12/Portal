import { ValidcheckerPipe } from './validchecker.pipe';

describe('ValidcheckerPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidcheckerPipe();
    expect(pipe).toBeTruthy();
  });
});
