import { InvoiceStatusColorPipe } from './invoice-status-color.pipe';

describe('InvoiceStatusColorPipe', () => {
  it('create an instance', () => {
    const pipe = new InvoiceStatusColorPipe();
    expect(pipe).toBeTruthy();
  });
});
